// Что вы думаете? Выполнится ли .catch? Поясните свой ответ.

new Promise(function(resolve, reject) {
  setTimeout(() => {
    throw new Error("Whoops!");
  }, 1000);
}).catch(alert);

// Да, выполнится, потому что функция вернёт промису reject

// Ошибка! Не выполнится. В данном примере ошибка генерируется не по ходу выполнения кода, а позже. Поэтому промис не может обработать её.
// Нужно вызвать reject от ошибки в асинхронном коде, а не за его пределами, тогда промис сможет ее обработать.